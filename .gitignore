# SwiftPM build products
.build/
DerivedData/

# Xcode (if ever opened there)
*.xcodeproj
*.xcworkspace
xcuserdata/

# user-saved presets + captured creatures/paths
# (presets/preset_003.json is force-tracked: it's the baseline brain+params
#  the headless studies --sweep/--map/--map3/--sdscan/--bistab all load)
presets/
captures/

# macOS
.DS_Store

# Python analysis env / tooling artifacts
.venv/
__pycache__/
*.pyc
.paper-index.json

# 3D velocity-field dumps (~25 MB each; written to data/ by --3dspec, regenerable)
vel3d_*.bin
data/vel3d_*.bin
